[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mgit"
version = "0.1.3"
description = "CLI for managing multiple git repositories"
authors = [{name = "Your Name", email = "your.email@example.com"}]
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "treeparse",
    "rich",
    "pyyaml",
    "questionary",
    "loguru",
    "pyperclip",
    "pydantic",
    "logfire>=3.17.0",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-mock",
    "pytest-cov",
]

test = [
    "pytest",
    "pytest-mock",
    "pytest-cov",
]

[project.scripts]
mgit = "mgit.cli:main"

[tool.pytest.ini_options]
addopts = "--cov=mgit --cov-fail-under=30"
pythonpath = "src"

[tool.coverage.run]
source = ["src"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
]

[dependency-groups]
dev = [
    "logfire>=3.17.0",
    "pytest>=8.3.5",
]
